# Dashboard 2 - Alternative Dashboard Design

This is a standalone Neobrutalist dashboard design prototype.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

**Note:** This is a standalone prototype. The integrated version is available in the main Stock Manager app at `/dashboard2`.
